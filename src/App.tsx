import React from "react";
import { styled } from "styled-components";

const AppWrapper = styled.div``;

function App() {
  return (
    <AppWrapper>
      <h1>Hello, World!</h1>
    </AppWrapper>
  );
}

export default App;
